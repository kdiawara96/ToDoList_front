import 'package:flutter/material.dart';

class Terminer extends StatelessWidget {
  const Terminer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Terminer"),
    );
  }
}
